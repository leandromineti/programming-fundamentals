---
phase: 01-toolchain-and-assembly-basics
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - Makefile
autonomous: false
requirements:
  - TOOL-02
  - TOOL-03

must_haves:
  truths:
    - "make check-all from project root runs all 7 exercises and reports 7 passed, 0 failed"
    - "make clean-all removes all build artifacts from all exercise directories and lib/"
    - "Every exercise directory has the complete four-file set: main.asm, expected_output, Makefile, README.md"
  artifacts:
    - path: "Makefile"
      provides: "Root Makefile with all 7 exercises in EXERCISES list"
      contains: "asm-07-array-iteration"
  key_links:
    - from: "Makefile"
      to: "01-registers/asm-01-mov/Makefile"
      via: "make -C $$ex check"
      pattern: "MAKE.*-C"
    - from: "Makefile"
      to: "03-memory/asm-07-array-iteration/Makefile"
      via: "make -C $$ex check"
      pattern: "asm-07-array-iteration"
---

<objective>
Finalize the root Makefile with all 7 exercises in the EXERCISES list, run the full suite verification, and confirm the phase is complete with a human verification checkpoint.

Purpose: This plan ensures the entire phase works end-to-end as an integrated suite. The root Makefile check-all is the single command that validates everything.
Output: Updated root Makefile, full-suite PASS confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-toolchain-and-assembly-basics/01-CONTEXT.md
@.planning/phases/01-toolchain-and-assembly-basics/01-01-SUMMARY.md
@.planning/phases/01-toolchain-and-assembly-basics/01-02-SUMMARY.md
@.planning/phases/01-toolchain-and-assembly-basics/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update root Makefile with all 7 exercises and verify full suite</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (current state -- created in Plan 01 with only asm-01-mov)
    - .planning/phases/01-toolchain-and-assembly-basics/01-RESEARCH.md (Pattern 2 -- Root Makefile with tolerant counting)
    - .planning/phases/01-toolchain-and-assembly-basics/01-CONTEXT.md (D-11 -- check-all requirement)
  </read_first>
  <action>
**Update the root `Makefile`** to include all 7 exercises in the EXERCISES variable (per D-11):

```makefile
EXERCISES := \
  01-registers/asm-01-mov \
  01-registers/asm-02-arithmetic \
  02-flow-control/asm-03-conditionals \
  02-flow-control/asm-04-loop \
  03-memory/asm-05-data-bss \
  03-memory/asm-06-addressing \
  03-memory/asm-07-array-iteration

.PHONY: check-all clean-all

check-all:
	@pass=0; fail=0; \
	for ex in $(EXERCISES); do \
	  if $(MAKE) -C $$ex check -s 2>/dev/null; then \
	    echo "PASS  $$ex"; pass=$$((pass+1)); \
	  else \
	    echo "FAIL  $$ex"; fail=$$((fail+1)); \
	  fi; \
	done; \
	echo ""; echo "Results: $$pass passed, $$fail failed"

clean-all:
	@for ex in $(EXERCISES); do \
	  $(MAKE) -C $$ex clean -s 2>/dev/null || true; \
	done
	rm -f lib/print_uint64.o
```

The EXERCISES list must contain exactly these 7 paths in this order (matching the topic group numbering from D-04).

After updating, run `make check-all` from the project root. Expected output:
```
PASS  01-registers/asm-01-mov
PASS  01-registers/asm-02-arithmetic
PASS  02-flow-control/asm-03-conditionals
PASS  02-flow-control/asm-04-loop
PASS  03-memory/asm-05-data-bss
PASS  03-memory/asm-06-addressing
PASS  03-memory/asm-07-array-iteration

Results: 7 passed, 0 failed
```

Then run `make clean-all` and verify all `.o` and binary files are removed. Then run `make check-all` again (rebuilds from clean state) to confirm the full build-from-scratch pipeline works.
  </action>
  <verify>
    <automated>cd /path/to/wsl2/project && make clean-all && make check-all 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - Root `Makefile` EXERCISES variable contains all 7 exercise paths
    - `Makefile` contains `01-registers/asm-01-mov`
    - `Makefile` contains `01-registers/asm-02-arithmetic`
    - `Makefile` contains `02-flow-control/asm-03-conditionals`
    - `Makefile` contains `02-flow-control/asm-04-loop`
    - `Makefile` contains `03-memory/asm-05-data-bss`
    - `Makefile` contains `03-memory/asm-06-addressing`
    - `Makefile` contains `03-memory/asm-07-array-iteration`
    - `make check-all` output contains "7 passed, 0 failed"
    - `make clean-all` followed by `make check-all` still produces "7 passed, 0 failed" (clean rebuild)
  </acceptance_criteria>
  <done>Root Makefile lists all 7 exercises. make check-all reports 7 passed, 0 failed. Full rebuild from clean state works.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification -- full suite and exercise quality</name>
  <files>Makefile</files>
  <action>
**CHECKPOINT: Human verification of the complete Phase 1 deliverable.**

What was built: 7 assembly exercises covering registers (mov, arithmetic), control flow (conditionals, loops), and memory (data/bss sections, addressing modes, array iteration). Each exercise has main.asm, expected_output, Makefile, and README.md. A root Makefile provides `make check-all` to verify the entire suite.

**Steps for the human to verify in WSL2 terminal:**

1. Navigate to the project directory (should be on WSL2 native filesystem, NOT /mnt/c/):
   ```bash
   cd ~/path/to/programming-fundamentals
   pwd  # confirm starts with /home/, NOT /mnt/c/
   ```

2. Run the full suite:
   ```bash
   make check-all
   ```
   Expected: 7 passed, 0 failed

3. Pick one exercise and run it interactively:
   ```bash
   cd 01-registers/asm-01-mov
   make run
   ```
   Confirm output displays on screen.

4. Open `01-registers/asm-01-mov/README.md` and verify:
   - Concept explanation is clear and in English
   - GDB starter kit section exists with 5 commands
   - "Try Modifying" suggestions are present

5. Try the GDB inspection (optional but recommended):
   ```bash
   cd 01-registers/asm-01-mov
   gdb ./main
   # At (gdb) prompt: break _start
   # At (gdb) prompt: run
   # At (gdb) prompt: info registers rax
   # At (gdb) prompt: si
   # At (gdb) prompt: quit
   ```
   Confirm GDB uses Intel syntax (no % prefixes on registers).

6. Verify clean rebuild:
   ```bash
   cd ~/path/to/programming-fundamentals
   make clean-all
   make check-all
   ```
   Expected: 7 passed, 0 failed (proves build-from-scratch works)

Resume signal: Type "approved" if all checks pass, or describe any issues found.
  </action>
  <verify>
    <automated>make check-all 2>&1 | grep "passed"</automated>
  </verify>
  <done>Human confirmed: all 7 exercises pass, README quality is acceptable, GDB uses Intel syntax, clean rebuild works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WSL2 filesystem | Root Makefile iterates exercise directories and runs make in each |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Denial of Service | check-all with tolerant counting | accept | A failing exercise does not abort the full run; the for-loop continues and tallies results |
</threat_model>

<verification>
1. `make check-all` from root prints "7 passed, 0 failed"
2. `make clean-all` removes all artifacts
3. Clean rebuild (`make clean-all && make check-all`) also passes
4. Human verifies exercise quality, README clarity, and GDB setup
</verification>

<success_criteria>
- Root Makefile EXERCISES list has all 7 exercises
- make check-all reports 7 passed, 0 failed
- Human confirms exercise readability and GDB setup
- Phase 1 success criteria from ROADMAP.md are satisfied:
  1. `make check` compiles and verifies any exercise with PASS/FAIL
  2. Student can write NASM programs with mov, add, sub, imul, idiv across register widths
  3. Student can implement branching (cmp + jcc) and loops with register counter
  4. Student can access .data/.bss and iterate arrays with indexed addressing
</success_criteria>

<output>
After completion, create `.planning/phases/01-toolchain-and-assembly-basics/01-04-SUMMARY.md`
</output>
